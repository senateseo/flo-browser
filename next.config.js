/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "files.edgestore.dev",
            "www.dpreview.com"
        ]
    }
}

module.exports = nextConfig
